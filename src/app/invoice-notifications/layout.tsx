import { AdminLayout } from '@/components/layout/admin-layout'

export default function InvoiceNotificationsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <AdminLayout>{children}</AdminLayout>
}

